from GUI import MyWindow

if __name__ == "__main__":
    # Initialize the window
    my_window = MyWindow()
    # Call the create_window method to create and customize the window, and add widgets
    my_window.create_window("Rule 5")

